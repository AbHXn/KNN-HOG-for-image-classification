3 folder named train_data, runner and library
train_data consists of training images
runner is the folder where the program execution occurs, place the test images inside the runner so that it will classfy and store in library folder
library folder is for placing the classified images

RULES:
images in trained data should be stored by name "classNum.png" ex: "Car1.png" and should not be stored in seperate folder in runner
